# Testing dependencies for Spark DI architecture

# Core testing framework
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
pytest-xdist>=3.0.0  # For parallel test execution

# Performance and monitoring
psutil>=5.9.0
memory-profiler>=0.60.0
line-profiler>=4.0.0

# Code quality
flake8>=6.0.0
black>=23.0.0
isort>=5.12.0
mypy>=1.0.0

# Coverage reporting
coverage>=7.0.0
coverage[toml]>=7.0.0

# Mocking and testing utilities
factory-boy>=3.2.0
faker>=18.0.0
responses>=0.23.0
httpx>=0.24.0  # For async HTTP testing

# Documentation testing
# doctest is built-in, no need to install

# Benchmarking
pytest-benchmark>=4.0.0

# HTML and PDF generation (for demo)
# weasyprint>=59.0  # For PDF generation (optional, may have issues on Windows)
jinja2>=3.1.0     # For template rendering
markdown>=3.4.0   # For markdown processing

# Additional utilities
tabulate>=0.9.0   # For formatted output
colorama>=0.4.6   # For colored terminal output
rich>=13.0.0      # For rich terminal output